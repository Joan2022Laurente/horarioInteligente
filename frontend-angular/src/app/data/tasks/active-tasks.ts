import { CourseAssignment } from '@domain/models/utp.model';

/**
 * Tareas activas del estudiante.
 * Vacío por defecto: se sincronizan dinámicamente desde el backend o la API académica UTP.
 */
export const ACTIVE_STUDENT_TASKS: CourseAssignment[] = [];
